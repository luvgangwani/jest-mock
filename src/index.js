import { add, divide, multiply, subtract } from "./calculator";

export const performAdd = (a, b) => add(a, b);
export const performSubtract = (a, b) => subtract(a, b);
export const performMultiply = (a, b) => multiply(a, b);
export const performDivide = (a, b) => divide(a, b);
